import { HiDirective } from './hi.directive';

describe('HiDirective', () => {
  it('should create an instance', () => {
    const directive = new HiDirective();
    expect(directive).toBeTruthy();
  });
});
